;; Estado global em ratoms: keyword, message, output, copied?, link-copied?.
;; Uptime timer: atom de segundos incrementado a cada 1s via setInterval.
;; format-uptime: converte segundos para string HH:MM:SS para exibição no status badge.
