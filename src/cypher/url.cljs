;; Encode/decode de texto cifrado para URL-safe base64 (sem +, /, = — usa -, _, sem padding).
;; share-url: gera URL completa com hash #msg=<base64url> para compartilhamento.
;; read-from-hash: lê e decodifica o payload do window.location.hash quando presente.
