;; View de leitura de link compartilhado — ativada quando hash contém #msg=.
;; Painel esquerdo: exibe texto cifrado (lido do hash) + campo KEYWORD + botão [ DECRYPT ].
;; Painel direito: output decriptado após aplicação da cipher com a keyword informada.
;; Header em modo alerta: "INCOMING TRANSMISSION" + status badge SIGNAL INTERCEPTED (pink).
